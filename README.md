# lab 4,1

Читает файл построчно — каждая строка это JSON: {"user_id": "1840e0b9d4", "category": "Продукты"}
Парсит JSON — json.loads() преобразует строку в словарь
Создает словарь {user_id: category} — последняя категория перезаписывает предыдущие
Выводит первые 2: 1840e0b9d4 Продукты





# lab4,2

purchases из Лаб 1 уже в памяти
Читает visit_log.csv построчно — user_id,source (большой файл)
first_row=True пропускает заголовок user_id,source
Фильтрует — только визиты пользователей с покупками
Записывает в funnel.csv: user_id,source,category
